n_workers: 2

ssh_config:
  ssh_server: "localhost"
  ssh_port: 2222
  username: "amine"
  priv_key_path: "./tests/keys/amine_rsa"
  pub_key_algo: "RSA-256"
  client_interface: "ib0"

proxy_config:
  - name: "http"
    service_ip: 127.0.0.1
    service_port: 8181
    local_ip: 127.0.0.1
    local_port: 8181
