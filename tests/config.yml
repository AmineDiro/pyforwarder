n_workers: 8

ssh_config:
  ssh_server: "localhost"
  ssh_port: 22
  username: "amine"
  priv_key_path: "tests/keys/amine_rsa"
  pub_key_algo: "RSA"
  client_interface: "ib0"

services:
  - name: "http"
    service_host: "localhost"
    service_port: 8181
    local_port: 8181
